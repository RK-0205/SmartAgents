# AI Chat Assistant

## Опис проекту
Веб-додаток для спілкування з AI-асистентами, що дозволяє створювати персоналізованих агентів та взаємодіяти з ними через зручний інтерфейс чату.

## Функціональні можливості
- 🤖 Створення персоналізованих AI-агентів
- 💬 Обмін повідомленнями в реальному часі
- 📁 Завантаження файлів для навчання агентів
- 🌐 Додавання веб-сторінок як джерел знань
- 💾 Збереження історії спілкування
- ⚙️ Налаштування та управління агентами

## Технічний стек
- Frontend:
  - JavaScript (vanilla)
  - HTML5
  - CSS3
- Backend:
  - Python
  - Flask
- Зберігання даних:
  - LocalStorage (для історії повідомлень)
  - Серверне зберігання (для агентів та контенту)


## Встановлення та запуск

### Передумови
- Python 3.8+
- pip (Python package manager)

### Кроки встановлення
1. Клонуйте репозиторій:
bash
git clone [url-репозиторію]

2. Встановіть залежності:
bash
pip install -r requirements.txt


3. Налаштуйте конфігурацію:
- Створіть файл `.env` на основі `.env.example`
- Додайте необхідні ключі API та налаштування

4. Запустіть додаток:
bash
python manage.py


## Використання

### Створення нового агента
1. Натисніть кнопку "+" для створення нового агента
2. Введіть ім'я та опис агента
3. Підтвердіть створення

### Навчання агента
1. Відкрийте налаштування агента
2. Завантажте файли або додайте URL веб-сторінок
3. Дочекайтесь обробки матеріалів

### Спілкування
1. Виберіть агента зі списку
2. Введіть повідомлення в поле вводу
3. Отримуйте відповіді в реальному часі

## API Endpoints

### Агенти
- `GET /api/agents` - отримання списку агентів
- `POST /api/agents` - створення нового агента
- `DELETE /api/agents/{id}` - видалення агента

### Повідомлення
- `POST /api/agents/{id}/ask` - надіслати повідомлення агенту

### Контент
- `GET /api/agents/{id}/content` - отримання контенту агента
- `POST /api/agents/{id}/file` - завантаження файлу
- `POST /api/agents/{id}/webpage` - додавання веб-сторінки
- `DELETE /api/agents/{id}/content/{content_id}` - видалення контенту

